import { default as Chromecast } from 'chromecast-api';
import Device from 'chromecast-api/lib/device';

import { Serve } from '../../types'

export function castToDevice(req: Serve.Request, res: Serve.Response) {

}